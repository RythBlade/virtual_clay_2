#pragma once

#include <windows.h>
#include <windowsx.h>

namespace virtual_clay
{
    char const* const g_className = "WindowClass";
    char const* const g_windowTitle = "Virtual Clay";

    LONG const g_defaultWindowWidth = 800;
    LONG const g_defaultWindowHeight = 600;

    LONG const g_defaultWindowPosX = 0;
    LONG const g_defaultWindowPosY = 0;
}